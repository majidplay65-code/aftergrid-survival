# قوانین ثابت این پروژه — هر Agent قبل از شروع کار باید این را بخواند

## Stack

Godot 4.7.2، GDScript با static typing کامل. هیچ نسخه‌ی دیگری استفاده نشود.

## معماری (تغییرناپذیر مگر با تأیید صریح مالک ریپو)

Resource برای داده، Node-based State Machine (core/state_machine/) برای رفتار، EventBus Autoload برای ارتباط بین سیستم‌ها.

## قانون طلایی ضد Hallucination

هر متد یا پراپرتی GDScript که مطمئن نیستی ۱۰۰٪ در Godot 4.7 واقعاً وجود دارد (مخصوصاً روی Vector2/Vector3/Node/CharacterBody3D)، قبل از استفاده با ابزار web search در مستندات رسمی docs.godotengine.org چک کن. گفتن "این متد باید وجود داشته باشد" کافی نیست؛ gdparse فقط سینتکس را چک می‌کند نه وجود API را — این دقیقاً همان باگی بود که در 0.6.0 رخ داد.

## قانون Git/PR

هرگز بدون تأیید صریح "من شخصاً در Godot تست کردم" از مالک ریپو، PR را merge نکن — حتی اگر کسی ادعا کند قبلاً merge شده؛ همیشه با GitHub API وضعیت واقعی PR را چک کن.

## پروتکل پایان‌سشن (Session-End Protocol)

هیچ سشنی بدون اجرای این سه قدم به پایان نمی‌رسد:

1. `git push` تمام کامیت‌های ساخته‌شده در همین سشن.

2. برای هر کامیتی که ادعا می‌شود push شده، بلافاصله با
   `gh api repos/{owner}/{repo}/commits/{sha}` تأیید کن که واقعاً روی
   GitHub قابل‌مشاهده است (نه فقط فرض بر اساس خروجی git push).

3. نتیجه‌ی این تأیید (SHA + وضعیت HTTP 200) را در آخرین پیام سشن
   صریحاً بنویس. اگر push یا تأیید ناموفق بود، این را با عنوان
   "⚠️ کار این سشن push نشد" برجسته اعلام کن، نه در میان توضیحات دیگر.

این قانون به‌خاطر از‌دست‌رفتن کامل دو دور کار (فاز ۶ Inventory یک‌بار،
و کار موسوم به «فاز ۷» یک‌بار دیگر) اضافه می‌شود — هر دو فقط چون در
سشن‌های قبلی commit شدند ولی هرگز push/تأیید نشدند.

## پروتکل میان‌سشن (لایه‌به‌لایه)

برای کارهای چندلایه‌ای (مثل فاز ۶: داده → منطق → اتصال → UI → تست)،
همین سه قدم را **بعد از هر لایه** انجام بده، نه فقط آخر کار. اگر سشن
در وسط کار قطع شود، حداقل لایه‌های push‌شده از دست نمی‌روند.

## ممیزی پروتکل (Protocol Audit) — ابتدای هر سشن

در ابتدای هر سشن جدید، علاوه بر چک وضعیت PR/برنچ‌ها/CI، این ممیزی الزامی است:

1. فهرست تمام بخش‌هایی از AGENTS.md که در سشن‌های قبلی «اضافه شد» گزارش
   شده‌اند را جمع کن.

2. برای هر مورد، **محتوای واقعی `main` روی GitHub** را بخوان و تأیید کن که
   آن بخش واقعاً آنجا هست — نه روی برنچ کاری، نه فقط داخل یک PR باز یا
   merge‌نشده:
   `gh api repos/{owner}/{repo}/contents/AGENTS.md?ref=main`

3. اگر بخشی روی `main` نبود، **قبل از هر کار دیگری** آن را ترمیم کن (در یک
   PR جدا) و طبق پروتکل پایان‌سشن push + تأیید API بگیر.

**چرا این قانون لازم است:** یک بخش می‌تواند کامل نوشته شود، commit شود و
حتی CI سبز بگیرد، ولی اگر PR‌اش هرگز merge نشود روی `main` وجود ندارد — و
سشن بعدی که AGENTS.md را می‌خواند آن را نمی‌بیند. یعنی قانونی که برای
جلوگیری از گم‌شدن کار نوشته شده، خودش گم می‌شود.

دو نمونه‌ی واقعی در همین ریپو:

- `672aead` («پروتکل پایان‌سشن و میان‌سشن») روی برنچ
  `arena/01a0953e-aftergrid-survival` ماند و هرگز merge نشد؛ فقط در سشن
  بعدی — وقتی صریحاً چک شد — با `b0bc2b9` و PR #10 ترمیم شد.
- `ae403b8` («فازهای کاندید آینده») فقط لوکال بود و روی GitHub (روی هیچ
  برنچی) پیدا نشد؛ کاملاً از دست رفت و محتوایش از نو نوشته شد
  (`d22b7cd` و PR #9).

**نکته:** شرط «روی `main` است» لازم است ولی کافی نیست — همیشه محتوای واقعی
`main` را بخوان و به حافظه یا خلاصه‌ی سشن قبلی تکیه نکن.

## فازبندی فعلی پروژه

فاز 0-6 انجام‌شده (Setup, Core, Player, Survival Loop, World, Threat, Inventory/Crafting). فاز بعدی تعریف نشده — منتظر تصمیم مالک ریپو. جزئیات کامل در README.md.

## فازهای کاندید آینده (کاندید، نه تعریف‌شده — تصمیم نهایی با مالک ریپو)

این سه مورد فقط گزینه‌های روی میز هستند و ترتیب/اولویتشان قطعی نشده؛ هیچ‌کدام تا انتخاب و اعلام صریح مالک ریپو شروع نمی‌شود:

- Threat variety — چند نوع دشمن با رفتار/آسیب/سرعت متفاوت
- Game Feel & Shareable Build — صدا، منو، دوربین، export
- World expansion — گسترش بلوک شهری و مناطق جدید

## محدوده

هر فاز را دقیقاً طبق چیزی که مالک ریپو در پرامپت هر بار مشخص می‌کند پیاده‌سازی کن؛ چیزی از فازهای بعدی زودتر اضافه نکن.
